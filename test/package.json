{
  "name": "phantomwright-test",
  "version": "1.0.0",
  "description": "Test suite for phantomwright npm package",
  "scripts": {
    "test": "node test_phantomwright.js",
    "install-browser": "npx patchright install chromium"
  },
  "dependencies": {
    "phantomwright-driver": "latest"
  }
}
